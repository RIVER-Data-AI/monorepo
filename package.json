{
  "name": "@river",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "concurrently": "concurrently --names \"frontend,backend\" --prefix-colors \"bgGreen.bold,bgMagenta.bold\"",
    "dev": "npm run concurrently -- \"npm --prefix packages/frontend run dev\" \"npm --prefix packages/backend run dev\"",
    "lint": "npm run lint --workspaces --if-present",
    "start": "npm run concurrently -- \"npm --prefix packages/frontend run start\" \"npm --prefix packages/backend run start\"",
    "test": "npm run test --workspaces --if-present",
    "prepare": "husky"
  },
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.7",
    "concurrently": "^9.0.1",
    "husky": "^9.1.6"
  }
}
